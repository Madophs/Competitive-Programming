#include <bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int holas;
	cin>>holas;
	while(holas--) cout<<"Hola mundo.\n";
	return 0;
}