#include <bits/stdc++.h>

using namespace std;

string unwist(string &str){
	
}

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int k;
	string twistedMsg;
	while(cin>>k && k && cin>>twistedMsg){
		
	}	
	return 0;
}
