#include <bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int val;
	while(cin>>val && val!=42)cout<<val<<endl;
	while(cin>>val);	
	return 0;
}
